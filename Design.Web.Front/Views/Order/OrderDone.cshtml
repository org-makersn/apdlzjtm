@model OrderT
@{
    Layout = "~/Views/Shared/_Printing_Layout.cshtml";
    DateTime date = (DateTime)Model.OrderDt;
    date = date.AddHours(12);
}


<div class="printText">
    <form id="print" name="print">
        <p class="txt_tit">주문이 완료 되었습니다.</p>
        <p class="txt2">
            @*    출력 요청이 ‘@Model.PrinterMemberName’님에게 전달되었습니다.<br>
            ‘@Model.PrinterMemberName’님이 요청을 수락하면 SMS와 e-mail로 알려드립니다.<br>
            만약 ‘@Model.PrinterMemberName’님이 12시간 이내에 요청을 수락하지 않을 경우<br>
            결제는 자동 취소되며 <strong>100% 환불</strong>됩니다.<br>
            <br>
            <strong>@(Model.PrinterMemberName)님의 프린팅 허락 후에는 주문을 취소할 수 없습니다.</strong>*@

            출력 요청금액 @(Model.TotalPrice)원을 아래의 계좌로 입금해주세요.<br />
            <label style="color:#ff7900">국민은행, 예금주 메이커스, 1232-232123-123123123</label><br />

            계좌정보는 <a href="/order/myorder">내 주문</a>에서 다시 확인할 수 있습니다.<br />
            <br />
            입금 확인 후 @(Model.PrinterMemberName)스팟님에게 주문이 전달되며,
            <br />
            스팟에서 12시간 이내에 요청을 수락하지 않을 경우
            <br />
             주문은 자동 취소되며 100% 환불됩니다.<br />
            <br />
            <strong>@(Model.PrinterMemberName) 스팟의 주문 승락 이후에는 주문을 취소 할 수 없습니다.</strong>
        </p>
        @*<div class="timeLeft mgT30">12:00:00</div>
        <input type="hidden" id="hfDate" value="@((date - DateTime.Now).TotalSeconds)" />*@
        <div class="btnS">
            <a href="/order/myorder" class="mgL395 btnG type8" style="background-color:#ff7900">결제내용 확인하기</a>
        </div>
    </form>
</div>


@section Scripts{
    <script type="text/javascript">
        //$(document).ready(function () {
        //    event_timer();
        //})

        //function event_timer() {
        //    var time_remain = $("#hfDate").val();
        //    rt = parseInt(time_remain);
        //    if (rt == 0) {

        //    } else {
        //        $(".timeLeft.mgT30").html(time_format(rt));
        //        time_remain = time_remain - 1;
        //        $("#hfDate").val(time_remain);
        //    }

        //    setTimeout("event_timer()", 1000);
        //}

        //function time_format(s) {
        //    var t;
        //    if (s > -1) {
        //        hour = Math.floor(s / 3600);
        //        min = Math.floor(s / 60) % 60;
        //        sec = s % 60;
        //        day = parseInt(hour / 24);
        //        //if (day > 0) {
        //        //    hour = hour - 24 * day;
        //        //    t = day + "天," + hour + ":";
        //        //}
        //        t = hour + ":";

        //        if (min < 10) { t += "0"; }
        //        t += min + ":";
        //        if (sec < 10) { t += "0"; }
        //        t += sec;
        //    }
        //    else { t = "0:00:0x"; }
        //    return t;
        //}

    </script>
}